<div class="panel panel-success">
  <div class="panel-heading">
    Location: <%= @location.name %>
    <%=link_to 'Edit', edit_location_path(@location), class: 'btn btn-warning btn-xs' %>
    <%=link_to 'Delete', location_path(@location), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-xs' %>
    <%=link_to 'Back to locations', locations_path, class: 'btn btn-primary btn-xs pull-right' %>

  </div>
  <div class="panel-body">
    <p>Description: Lorem ipsum dolor sit amet, sea te animal voluptaria. Cu epicuri evertitur inciderint mel. Cu qui dolorem molestiae. Vivendo lobortis assueverit usu et.</p>
  </div>

  <table class="table table-hover">
    <tr>
      <th>Street address</th>
      <th>City</th>
      <th>State</th>
      <th>Region</th>
      <th>Country</th>
      <th>Postal code</th>
    </tr>

    <tr>
      <td><%= @location.street_address %></td>
      <td><%= @location.city %></td>
      <td><%= @location.state %></td>
      <td><%= @location.region %></td>
      <td><%= @location.country %></td>
      <td><%= @location.postal_code %></td>
    </tr>
  </table>
</div>
